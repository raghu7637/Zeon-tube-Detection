path: dataset  # dataset root dir
train: images/train  # train images 
val: images/val  # val images 

# Classes
names:
  0: tube

# Keypoints
kpt_shape: [2, 3] # [number of keypoints, number of dim (x, y, visibility)]
